apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mirrored-registry.fullname" . }}-test-connection"
  labels:
    {{- include "mirrored-registry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mirrored-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

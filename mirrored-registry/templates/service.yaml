apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirrored-registry.fullname" . }}
  labels:
    {{- include "mirrored-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mirrored-registry.selectorLabels" . | nindent 4 }}
